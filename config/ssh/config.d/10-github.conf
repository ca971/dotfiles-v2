# ═══════════════════════════════════════════════════════════════════════════════
# @file config/ssh/config.d/10-github.conf
# @description GitHub SSH — multi-key with automatic fallback
# @since 1.0.0
#
# Keys (in priority order):
#   1. id_github_ed25519   — Primary GitHub key
#   2. id_fallback_ed25519 — Fallback key
# ═══════════════════════════════════════════════════════════════════════════════

# ── GitHub — auto-failover ───────────────────────────────────────────────────
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_github_ed25519
    IdentityFile ~/.ssh/id_fallback_ed25519
    IdentitiesOnly yes
    AddKeysToAgent yes
    PreferredAuthentications publickey

# ── GitHub — force primary key ──────────────────────────────────────────────
Host github-primary
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_github_ed25519
    IdentitiesOnly yes

# ── GitHub — force fallback key ─────────────────────────────────────────────
Host github-fallback
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_fallback_ed25519
    IdentitiesOnly yes
